package business;

import data.interfaces.Graph;

public class Algorithm {

        /**
         * Runs in O(|V|^2) time.
         * 
         * @param graph
         * @param source
         * @param target
         * @return
         */
        public int DijkstraAlgorithm(final Graph graph, final int source,
                        final int target) {
                return 1;
        }

        public int FloydWarshallAlgorithm(final Graph graph,
                        final Object source, final Object target) {
                return 1;
        }

        public int getGraphConnectivity(final Graph graph, final int source,
                        final int target) {
                return 1;
        }

        public int KruskalAlgorithm(final Graph graph) {
                return 1;
        }

        public int MaxFlowAlgorithm(final Graph graph) {
                return 1;
        }

        public int PrimAlgorithm(final Graph graph) {
                return 1;
        }
}
